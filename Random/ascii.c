#include<stdio.h>

void main(){
	int n;
	float b;
	double c;
	char ch;
	printf("The storage of integer data type is %d\n",sizeof(n));
	printf("The storage of float data type is %d\n",sizeof(b));
	printf("The storage of double data type is %d\n",sizeof(c));
	printf("The storage of character data type is %d\n",sizeof(ch));
	func(2,4);
}
